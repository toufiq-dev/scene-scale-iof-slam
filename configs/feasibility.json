{
  "experiment": "synthetic-feasibility-fixed",
  "description": "Corrected feasibility gate with the G3 confound fixed and round-2/round-3 review fixes applied: estimated (corrupted) motion by default, oracle vs runtime-available baselines, GRU + classical covariance-propagation baselines, per-sequence (within-sequence) failure thresholds as the primary early-warning metric, full input-combination ablation matrix, a generator stress matrix (degraded reliability, jump failures, realistic depth corruption), the official-protocol IOF machinery (trajectory alignment + BIC depth-distribution integration) with a raw-vs-official comparison leg, a distributional FlowAUC head, and a label-noise sensitivity sweep over IOF sample counts.",
  "seeds": [0, 1, 2],
  "data": {
    "n_train": 60,
    "n_val": 15,
    "n_test": 15,
    "seq_len": 80,
    "seed_base": 1000
  },
  "generator": {
    "error_model": "decoupled",
    "motion_source": "estimated",
    "reliability_mode": "clean",
    "jump_prob": 0.0,
    "depth_corruption": "gaussian",
    "depth_choices": [0.5, 0.8, 1.2, 2.0, 4.0, 8.0, 15.0, 30.0],
    "rot_innov_weight": 0.3,
    "stress_variants": [
      "true motion (pre-fix input)",
      "reliability: delayed",
      "reliability: noisy",
      "reliability: miscalibrated",
      "reliability: masked",
      "reliability: intermittent",
      "jump failures",
      "realistic depth corruption"
    ]
  },
  "windows": {
    "k": 5,
    "h": 5,
    "num_samples_iof": 200,
    "samples_sweep": [100, 200, 500, 1000]
  },
  "flowauc_head": {
    "kind": "distributional (percentile bins over 0-100 px)",
    "bins": 9,
    "thresholds_px": [1, 5, 20],
    "official_auc_grid": [0, 100, 41]
  },
  "official_iof_leg": {
    "alignment": "Umeyama Sim(3) + Kabsch SO(3)",
    "depth_distribution": "BIC-selected Gaussian mixture (max 3 components)",
    "integration": "log-spaced quadrature (0.1-60 m, 32 nodes)",
    "sequences": 3
  },
  "model": {
    "kind": "mlp",
    "hidden": 128,
    "epochs": 60,
    "patience": 8,
    "lr": 0.001,
    "batch": 256,
    "temporal_baseline": {
      "kind": "gru",
      "hidden": 64,
      "epochs": 150,
      "patience": 12,
      "lr": 0.003
    }
  },
  "baselines": [
    "constant",
    "persistence (oracle)",
    "ridge (same features)",
    "classical cov-prop (blind)",
    "gru (temporal, same features)",
    "analytic 2-stage (phys-explicit)",
    "mlp full h=0 (current)"
  ],
  "ablations": [
    "motion-only",
    "depth-only",
    "reliability-only",
    "motion+depth",
    "motion+reliability",
    "depth+reliability",
    "full"
  ],
  "failure_definitions": [
    "global_tau",
    "per_seq_tau (within-sequence, PRIMARY)",
    "robust_zscore",
    "depth_normalized"
  ],
  "failure_threshold_percentile": 75,
  "metrics": ["rmse", "nrmse", "med_spearman", "auroc", "med_auroc", "wauroc", "med_wauroc", "ap"]
}
